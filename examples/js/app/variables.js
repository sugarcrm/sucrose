// jQuery.my variables
var Manifest, Config, baseUI;

// jQuery references
var $demo,
    $chart,
    $config,
    $data,
    $example,
    $form,
    $index,
    $menu,
    $options,
    $picker,
    $table,
    $title;

// Application scope variables
var chartStore,
    chartType,
    fileCatalog,
    localeData,
    tooltip;

// Visualization data
var Data,
    chartData,
    tableData;

// Data dependent variables
var xIsDatetime,
    yIsCurrency;
