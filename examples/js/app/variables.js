// jQuery.my variables
var Manifest, Data, baseUI;

// jQuery references
var $title,
    $picker,
    $select,
    $index,
    $demo,
    $options,
    $form,
    $example,
    $chart,
    $menu;

// Application scope variables
var chartType,
    chartStore,
    chartData,
    tootip;
